Storage API
* window.sessionStorage (object)
* transient
* Key/Value store
* Both Key & Value should be strings
* API
    - setItem(key, value)
    - removeItem(key)
    - getItem(key) // => value
    - clear()
    - key(index) //=> key
    - length
    

    